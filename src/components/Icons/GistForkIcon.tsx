import { SvgIcons } from '@/assets';
import { SvgIcon, type SvgIconProps } from '@mui/material';

export const GistForkIcon = (props: SvgIconProps) => (
  <SvgIcon component={SvgIcons.GistForkIcon} inheritViewBox {...props} />
);
