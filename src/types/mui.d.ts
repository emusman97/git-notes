export declare module '@mui/material/styles' {
  interface Palette {
    searchBorder: Palette['primary'];
    tableHeader: Palette['primary'];
    fileNameContainerBg: Palette['primary'];
    addFileButton: Palette['primary'];
  }

  interface PaletteOptions {
    searchBorder?: PaletteOptions['primary'];
    tableHeader: PaletteOptions['primary'];
    fileNameContainerBg: PaletteOptions['primary'];
    addFileButton: PaletteOptions['primary'];
  }
}
